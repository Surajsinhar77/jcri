Document for Keeps notes


1. element.classlist.add
2. element.insertAdjecentHTML(WhereYouPlace ,variable);
3. to remove node use Element.remove()

#NOTE : addEventListiner is work better than Id as Compare to  2class of same element    (quearySelector)